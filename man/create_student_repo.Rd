% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_student_repo.R
\name{create_student_repo}
\alias{create_student_repo}
\title{Create one student's repository and push starter files}
\usage{
create_student_repo(student, files_dir, prefix = "hw1", permission = "push")
}
\arguments{
\item{student}{GitHub username (handle).}

\item{files_dir}{Local folder whose contents are pushed into the repo.}

\item{prefix}{Assignment prefix. The repo is named prefix-student.}

\item{permission}{Collaborator permission. "push" lets the student
commit work. "admin" also lets them manage the repo.}
}
\value{
A one row data frame with student, repo, and status. Status is
one of "ok", "exists", "create_error", or "setup_error".
}
\description{
Creates a fresh private repository in the organization, pushes every
file in a local folder into it through the contents API, and adds the
student as a collaborator. If the repository already exists it is left
alone and the function reports that, so the call is safe to repeat.
}
\examples{
\dontrun{
set_org("ISTA421INFO521")
create_student_repo("octocat", "hw1_files", prefix = "hw1")
}
}
